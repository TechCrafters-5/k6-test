# k6 压测落地执行计划

> 背景：Popstar 平台（~20 个 Spring Boot 微服务，Dubbo RPC + HTTP 网关，阿里云 ECS 部署）
> 决策依据：`loadtest-plan.html` 技术调研文档 —— 日常压测用 k6（HTTP 网关层），重大版本验证用阿里云 PTS（多地域真实性验证）
> 当前状态：`loadtest/` 目录骨架已生成（`lib/auth.js`、`scenarios/_template.js`、`run.sh`），需要按此文档继续落地

---

## Phase 0：环境与角色划分（已决策，仅供对齐，无需执行）

| 环境 | 日常压测机器 | 网络路径 | 目的 |
|---|---|---|---|
| 测试环境 | Mac 本机 | 公网 | 调脚本、验证跑通，不追求数据准确 |
| 测试环境（一次性） | 压测 ECS（临时切到测试角色） | 公网 | 验证换机器/换网络路径后结果一致 |
| 生产环境 | 压测 ECS（固定放生产集群） | 内网 | 日常压测，出正式报告，数据要准 |
| 生产环境（补充） | Mac | 公网 | 模拟真实用户网络体验，不作为报告标准数据 |
| 生产环境（大版本） | 阿里云 PTS | 全国多地域公网 | 架构变更/大促前的真实性验证 |

---

## Phase 1：脚本调通（Mac 本机，测试环境，公网）

**目标：先解决"对不对"，不解决"准不准"**

- [ ] 确认测试环境网关的公网访问地址，替换 `lib/auth.js` 中 `BASE_URL` 占位值
- [ ] 确认测试账号获取 token 的实际方式，二选一：
  - [ ] 方案 A：测试环境提供压测专用登录接口（账号密码换 token）——按此改写 `lib/auth.js` 的 `login()`，替换实际接口路径、请求体字段、响应体 token 字段名
  - [ ] 方案 B：离线预生成一批 token（不现场走微信 code2session）——把 `login()` 换成从环境变量 / 本地文件读取预置 token 列表
- [ ] 确认鉴权头格式（`Authorization: Bearer <token>` 还是自定义 header 名），修正 `authHeaders()`
- [ ] 复制 `scenarios/_template.js` 为第一个真实场景脚本（如 `order-query.js`，建议从**只读接口**开始，暂不碰下单等写接口），替换实际业务接口路径
- [ ] 本机跑通：`BASE_URL=<测试环境公网地址> LOAD_TEST_USER=xxx LOAD_TEST_PASS=xxx ./run.sh scenarios/order-query.js`
- [ ] 确认响应状态码、阈值（`http_req_duration p(95)<500`、`http_req_failed rate<0.01`）符合预期，排除明显 bug
- [ ] （如需要）验证测试环境网关是否有 WAF / 防刷策略会拦截压测流量，若有需要申请白名单或调整策略

**验收标准**：脚本能在 Mac 上稳定跑完一轮，无脚本层面的报错（登录失败、字段解析错误、URL 404 等）

---

## Phase 2：压测 ECS 采购与安全加固（与 Phase 1 并行）

**目标：机器和安全边界准备好，不依赖 Phase 1 结果**

- [ ] 采购一台独立 ECS（不与任何业务实例复用），确定最终固定放置于**生产集群**网络分区（内网默认与生产互通）
- [ ] 安装 k6（根据实际系统发行版，CentOS/Alibaba Cloud Linux 用 `dnf install https://dl.k6.io/rpm/repo.rpm && dnf install k6`；Ubuntu/Debian 用 apt + k6 官方 gpg key）
- [ ] 安全组按最小权限配置：
  - [ ] 仅放行该机器到网关 / 被测服务所需的端口，不整段 VPC 互通
  - [ ] SSH 管理端口仅允许工程师本人 IP 白名单，不对公网开放
  - [ ] 确认压测机不承载任何生产长期凭证（数据库密码、AK/SK），token 现取现用不落盘
- [ ] 检查 `ulimit -n`（文件描述符上限），建议调至 4096 以上
- [ ] 资产清单 / 网络拓扑图中登记这台机器为"压测工具机"，标注用途和权限范围（等保测评/复测需要能说清楚）
- [ ] k6 内置 Dashboard 默认只绑定 127.0.0.1，如需远程查看，安全组临时开放来源 IP 白名单，用完关闭

**验收标准**：`k6 version` 正常输出；SSH 仅工程师本人可连；安全组规则可审计、无多余放行

---

## Phase 3：ECS 上验证测试环境（一次性，公网）

**目标：验证"换机器/换网络路径"没有环境特有的坑，非常态化任务**

- [ ] 将 Phase 2 的 ECS 临时用于测试环境验证：`BASE_URL=<测试环境公网地址>` 跑 Phase 1 已调通的场景脚本
- [ ] 对比 Mac 本机跑出的结果和 ECS 跑出的结果，确认脚本行为一致（不要求延迟数据一致，只要求逻辑正确、无报错）
- [ ] 确认完成后，此步骤不需要再重复执行（测试环境目的已达成：验证脚本没 bug）

**验收标准**：ECS 上跑通同一脚本，无环境特有报错

---

## Phase 4：切换到生产角色，正式日常压测（内网）

**目标：出准确、可信的压测数据**

- [ ] 确认 ECS 已在生产集群网络分区，`BASE_URL` 改为生产网关**内网**地址
- [ ] 场景范围收窄确认：
  - [ ] 只读类接口（门店列表、订单查询等）→ 可直接常态化跑
  - [ ] 写操作接口（下单等）→ 需先确认是否有专用测试商户/测试门店数据隔离机制（避免污染生产订单表、报表、真实支付链路），无隔离机制前不在生产常态化跑
- [ ] 按 `scenarios/` 目录规范补充更多业务场景脚本（一个脚本对应一个真实用户行为）
- [ ] 阈值门禁（`thresholds`）按核心接口 SLA 配置，为后续接入 CI 做准备（本阶段暂不接 CI）
- [ ] （补充测试）Mac 不定期跑一次生产网关的公网压测，作为"真实用户网络体验"的参考数据，明确不计入正式压测报告

**验收标准**：产出第一份日常压测数据（p95/p99、错误率、RPS），只读接口覆盖完成

---

## Phase 5：待后续推进（暂不在本次范围）

- [ ] CI/CD 集成（GitHub Actions，阈值自动 pass/fail）
- [ ] Dubbo RPC 层日常验证方案（JMeter + Dubbo 插件，按需临时跑）
- [ ] 阿里云 PTS 首次接入（Swagger 覆盖率检查、Java Agent 探针、VUM 额度确认）——触发条件为核心链路大版本升级 / 架构变更 / 大促前

---

## 需要人工决策/提供信息的关键点（Claude Code 无法自行判断）

1. 测试环境和生产环境网关的实际公网/内网地址
2. 压测专用账号的 token 获取方式（方案 A 还是方案 B，见 Phase 1）
3. 生产环境写接口（下单类）是否已有测试数据隔离机制，没有的话由谁负责补
4. ECS 具体规格与所属生产网络分区的安全组基线（由工程师本人把控，见 Phase 2）
